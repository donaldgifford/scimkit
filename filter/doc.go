// Package filter implements RFC 7644 §3.4.2.2 filter and §3.5.2 path
// parsing into a typed AST, evaluation of filter expressions against
// scim.Resource values, and a builder for constructing correctly quoted
// filter strings on the client side.
package filter
